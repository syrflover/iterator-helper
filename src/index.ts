import './types/array';
import { logger } from './logger';

export { IteratorHelper } from './iterator';

/* const a = [1, 2, 3, 4, 5, 6, 7, 8].iter();

a.filter((e) => e % 2 === 0)
    .map((e) => e + 1)
    .collect(); */
